import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ message: 'Session API stub' })
}

export async function DELETE() {
  return NextResponse.json({ message: 'Session API stub' })
}
